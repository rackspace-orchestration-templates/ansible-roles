# description "Start and Stop Parse Dashboard"

start on runlevel [2345]
stop on runlevel [^2345]

console log
chdir /home/{{ parse_user }}
setuid {{ parse_user }}

respawn
respawn limit 20 5

exec parse-dashboard --config parse-dashboard-config.json
