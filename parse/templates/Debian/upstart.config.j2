# description "Start and Stop Parse Dashboard"

start on runlevel [2345]
stop on runlevel [^2345]

console log
chdir /home/{{ parse_user }}/dashboard
setuid {{ parse_user }}

respawn
respawn limit 20 5

exec bin/parse-dashboard
