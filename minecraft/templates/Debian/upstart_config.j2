# description "Start and Stop Minecraft-Server"

start on runlevel [2345]
stop on runlevel [^2345]

console log
chdir /home/minecraft
setuid minecraft

respawn
respawn limit 20 5

exec /usr/bin/java -Xms{{ minecraft_memory_min }} -Xmx{{ minecraft_memory_max }} -jar minecraft-server.jar nogui
