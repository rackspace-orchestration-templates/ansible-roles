# description "Start and Stop Ghost"

start on runlevel [2345]
stop on runlevel [^2345]

console log
chdir {{ ghost_dir }}
setuid {{ ghost_user }}

respawn
respawn limit 20 5

exec npm start --production
